export type ButtonClickEvent = React.MouseEvent<HTMLButtonElement, MouseEvent>
export enum E_Queries {
  products = 'products',
  collections = 'collections',
  text_blocks = 'text_blocks',
  orders = 'orders',
  photos = 'photos',
}
